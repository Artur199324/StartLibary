jdk:
  - openjdk17

before_install:
  - ./gradlew clean

install:
  - ./gradlew assemble

script:
  - ./gradlew build
  - ./gradlew publishReleasePublicationToMavenLocal

publish:
  target: ./StartLibary/build/outputs/aar