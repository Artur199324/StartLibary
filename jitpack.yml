jdk:
  - openjdk11

before_install:
  - sdk install java 17.0.11-oracle
  - sdk use java 17.0.11-oracle

install:
  - ./gradlew clean assemble

script:
  - ./gradlew publishToMavenLocal
  - ls -R build/publications/maven